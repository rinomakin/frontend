import React from "react";
import { AiFillPlusCircle } from "react-icons/ai";

const Tambah = ({ ...Rest }) => {
  return (
    <div>
      <AiFillPlusCircle {...Rest} />
    </div>
  );
};

export default Tambah;
